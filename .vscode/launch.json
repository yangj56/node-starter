{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 9222,
      "request": "attach",
      "type": "chrome",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "movie-app",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm test -- index.test.ts",
      // "command": "npm run dev",
      "cwd": "${workspaceFolder}/"
    }
  ]
}
