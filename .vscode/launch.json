{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 9222,
      "request": "attach",
      "type": "chrome",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "node-starter",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      // "command": "npm run dev",
      "cwd": "${workspaceFolder}/"
    }
  ]
}
